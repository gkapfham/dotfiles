[colors]
; Define colors to be centered by dark grey
; Some of these colors are not used now
background = #1c1c1c
background-alt = #c6c6c6
foreground = #c1c1c1
foreground-alt = #a8a8af
primary = #444444
secondary = #e60053
alert = #616161
green = #009300
yellow = #E95420
red = #f90000
blue = #0a6cf5
sep-bright = #5a5a5a

; Define all of the configuration details for
; the "mybar" polybar custom-designed for display
; at the top of the screen for a polybar that 
; runs on a Framework 13 laptop with the 2.2K
; screen that has the rounded corners

[bar/mybar]
; Define format for mybar
monitor = ${env:MONITOR:}
width = 100%
height = 60
fixed-center = true

; Set the two basic colors
background = ${colors.background}
foreground = ${colors.foreground}

; Make the lines the same as background
line-size = 12
line-color = ${colors.background}
border-size = 0

; Don't use padding by default
padding-left = 0
padding-right = 0

; Set 1 unit of margin by default
module-margin-left = 12px
module-margin-right = 8px
module-margin-center = 5px

; Specify the main font
font-0 = "Maple Mono NF:size=20:weight=bold;5"

; Specify the icon font
font-1 = "Maple Mono NF:size=20;5"

; Specify the emphasis font for the i3 workspaces
font-2 = "Maple Mono NF:size=20:weight=bold;5"

; Left panel: display the i3 workspaces
modules-left = i3

; Middle panel: display the wireless network, battery, cpu, temperatures, fan, memory, and filesystem
modules-center = wireless-network battery cpu temperature temperature-gpu temperature-ssd fan memory filesystem

; Right panel: display the backlight, pulseaudio, and date
modules-right = backlight pulseaudio date

; Allow the mouse to click and scroll
cursor-click = pointer
cursor-scroll = ns-resize

; Specify some global redraw settings
[settings]
screenchange-reload = true

; Specify some global margin settings
[global/wm]
margin-top = 5
margin-bottom = 5

; Display details about the wired network;
; note that this is not yet configured
; correctly to work with the new wired network
[module/wired-network]
type = internal/network
interface = enp6s0
interval = 15
udspeed-minwidth = 0
format-connected-prefix = " "
format-connected = <label-connected>
format-disconnected = <label-disconnected>
label-connected =  %upspeed:8% %downspeed:8%

; Display details about the wireless network
; Note: the content of this section has been modified;
; see the commented-out section below for the original
[module/wireless-network]
type = internal/network
interface = wlp1s0
interval = 15
udspeed-minwidth = 0
format-connected-prefix = ""
format-connected = <label-connected>
format-disconnected = <label-disconnected>
label-connected =   %essid:0:16:…% 󰒢 %signal:2%
label-packetloss =   %essid% %signal:2% %upspeed:9% %downspeed:9%

; Display the i3 workspaces; note that the
; spacing in the format prefix and the icon
; ensures that the existence of the rounded
; corners in the screen are not readily apparent
[module/i3]
type = internal/i3
format = <label-state><label-mode>
format-prefix = "   "
index-sort = true
wrapping-scroll = true
enable-click = true
pin-workspaces = true
strip-wsnumbers = true

; Configure the display of the mode indicator
label-mode = %mode%
label-mode-padding = 5px
label-mode-margin-left = 5px
label-mode-margin-right = 5px
label-mode-foreground = ${colors.foreground}
label-mode-background = ${colors.primary}

; Focused = Active workspace on focused monitor
; This expands to give the full name of the workspace
; Using a separate process will dynamically adjust
; these names depending on the program currently in use
; Previous focused label config (commented out for reference):
;   label-focused = %name%
;   label-focused-font = 3
;   label-focused-background = ${colors.primary}
;   label-focused-underline = ${colors.primary}
;   label-focused-foreground = ${colors.background-alt}
;   label-focused-padding = 1
label-focused = %name%
label-focused-font = 3
label-focused-background = ${colors.primary}
label-focused-overline = ${colors.background}
label-focused-underline = ${colors.background}
label-focused-foreground = ${colors.background-alt}
label-focused-padding = 1

; Unfocused = Inactive workspace on any monitor
label-unfocused = %name%
label-unfocused-padding = 1

; Visible = Active workspace on unfocused monitor
label-visible = %name%
label-visible-background = ${self.label-focused-background}
label-visible-overline = ${self.label-focused-overline}
label-visible-underline = ${self.label-focused-underline}
label-visible-padding = ${self.label-focused-padding}

; Urgent = Workspace with urgency hint set
label-urgent = %name%
label-urgent-background = ${colors.alert}
label-urgent-padding = 1

; Display the use of the processor
[module/cpu]
type = internal/cpu
interval = 10
format-margin = 5px
format-prefix = " "
label =  %percentage:2%%

; Thermal block — one separator at the start, none between the three temps
[module/temperature]
type = internal/temperature
interval = 10
thermal-zone = 0
hwmon-path = /sys/class/hwmon/hwmon3/temp2_input
format = <label>
label =  %temperature-c:2%

[module/temperature-gpu]
type = internal/temperature
interval = 10
thermal-zone = 0
format-prefix = "󰪤 "
hwmon-path = /sys/class/hwmon/hwmon1/temp1_input
format = <label>
label = %temperature-c:2%

[module/temperature-ssd]
type = internal/temperature
interval = 10
thermal-zone = 0
format-prefix = "󰋊 "
hwmon-path = /sys/class/hwmon/hwmon0/temp1_input
format = <label>
label = %temperature-c:2%

; Display the fan speed
[module/fan]
type = custom/script
interval = 10
format-prefix = " "
label = %output%
exec = ~/.config/polybar/scripts/fan

; Previous internal/memory configuration (commented out for reference):
;   type = internal/memory
;   label = 󱙋 %gb_used% of 󱤟 %gb_total%

; Display the use of the memory
[module/memory]
type = custom/script
interval = 10
format-margin = 5px
format-prefix = " "
format-underline = ${colors.background}
label = %output%
exec = ~/.config/polybar/scripts/memory

; Display details about the filesystem
[module/filesystem]
type = internal/fs
mount-0 = /
interval = 60
format-margin = 5px
format-mounted-prefix = " "
format-mounted-prefix-foreground = ${colors.sep-bright}
format-mounted = <label-mounted>
label-mounted = 󰋊 %mountpoint%: %percentage_used%% of %total%

; Display the date
[module/date]
type = internal/date
format-prefix = " "
interval = 60
date =  %b %d
time = "%H:%M "
label = %date% %time%
format = <label>
format-underline = ${colors.background}

; Display the level of the audio
[module/pulseaudio]
type = internal/pulseaudio
sink = alsa_output.pci-0000_c1_00.6.pro-output-0

format-volume = <label-volume>
label-volume =  %percentage:3%%
label-volume-foreground = ${root.foreground}
format-volume-underline = ${colors.background}

format-muted-prefix = "󰝟 "
label-muted = %percentage:2%%
format-muted-underline = ${colors.background}

; Display backlight details
; Note that this works even though i3 uses light
; but only if you set the use-actual-brightness to false
; as a workaround to incorrect display of a maximum
[module/backlight]
type = internal/backlight
card = amdgpu_bl1
use-actual-brightness = false
interval = 15
format = <ramp> <label>
format-prefix = " "
label = %percentage:3%%

ramp-4 = 󰃠
ramp-3 = 󰃝
ramp-2 = 󰃟
ramp-1 = 󰃟
ramp-0 = 󰃞

; Display the level of the battery
[module/battery]
type = internal/battery
battery = BAT1
adapter = ACAD
full-at = 100
time-format = %H:%M
interval = 30

label-charging = %percentage:3%% 󰥔 %time%  %consumption%
format-charging-prefix = "󰁹"
format-charging = <label-charging>
format-charging-underline = ${colors.background}

label-discharging = %percentage:3%% 󰥔 %time%  %consumption%
format-discharging-prefix = "󰁹"
format-discharging = <label-discharging>
format-discharging-underline = ${colors.background}

label-full = %percentage:3%%
format-full-prefix = "󰁹 "
format-full-underline = ${colors.background}
